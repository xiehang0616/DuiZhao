# DuiZhao 使用流程

DuiZhao 用于比较同一问题下多个模型的输出，并通过人工评价与固定用例测评辅助选型。开始前请完成 [安装与运行](docs/安装与运行.md)。

## 1. 配置并验证模型

进入“模型配置”，添加预设或自定义模型。核对服务商、模型 ID、服务地址和能力；同名模型通过不同服务接入时，可以使用独立连接。

填写 API Key 并保存。独立连接可以使用单独密钥，也可以按配置继承已有服务商级密钥。密钥由后端保存，界面不回显原值。

文本类连接测试会发起最小真实请求，可能消耗额度。图片与视频的连接测试会提示跳过，需要切换到对应内容类型，实际生成一次验证。预设只是连接模板，不代表该模型已通过逐一实测。

## 2. 发起第一次对比

以“请为一款咖啡机写一段 100 字以内的介绍”为例：

1. 选择文本类型，并勾选 2 个已配置、可用的模型。
2. 填写相同的问题；有共同角色、格式或约束时，写入系统提示词。
3. 发送后分别观察各模型卡片。一个模型较慢或失败，不必等待它才能阅读其他结果。
4. 查看输出来源、耗时和 Token。演示内容不计为真实能力证据，未采集数据也不能当作零值。
5. 根据是否符合字数、事实与业务要求，填写单项评价、1–5 分和备注。

普通文本、图片理解与代码对比通常要求 2–9 个模型；图片和视频生成至少选择 1 个。继续在同一话题提问会保留话题记录，但当前请求不会自动拼入完整历史上下文，需要时应在问题中补充关键背景。

## 3. 使用其他内容类型

| 内容类型 | 操作流程 | 核对重点 |
| --- | --- | --- |
| 图片生成 | 描述画面，选择模型与规格；可上传参考图 | 主体、文字、构图与业务要求；规格支持取决于服务商 |
| 图片理解 | 上传图片，写明需要识别或分析的问题 | 模型须支持图片理解，问题中明确检查对象 |
| 代码 | 描述目标、语言、输入输出及边界 | 当前展示代码不等于已执行或测试，应另行运行验证 |
| 视频 | 描述镜头、动作，选择规格和时长；可上传首帧 | 时序、运动与参考一致性；图生视频使用首张参考图 |

图片和视频结果支持预览。后端会尝试保存生成媒体，但应确认落盘和引用可用。停止本地等待不一定取消服务商已经接收的生成任务。

## 4. 评价与导出

普通对比可填写“更好 / 较差”、1–5 分、单项备注及本轮备注。报告菜单导出当前轮次；话题顶部导出入口可保留整个话题的表格记录。

| 格式 | 适用场景 |
| --- | --- |
| Excel / CSV | 筛选和比较各模型结果、用量、耗时与评价 |
| Markdown | 阅读本轮回答与评价，整理成可分享的报告 |

人民币费用来自内置价格表估算，不是实时账单。模型未收录、用量缺失或规格倍率不适用时，应回查服务商记录。系统生成的分析不能代替人工业务判断，分享前也应检查报告正文和附件是否含私人内容。

## 5. 保存常用问题

在“我的题库”保存问题、系统提示词和参考材料，可用于快速发起下一次对比。准备正式测评时，同时定义验收条件、严重错误和评分维度，避免看到模型答案后再改变标准。

## 6. 开展人工测评

1. 创建测评任务，选择同一模块的用例与模型，明确版本、测试条件、评价人和权重。
2. 在实际模型工具中完成测试，再通过“新增独立测评”录入输出、证据、状态及已采集的耗时、费用与 Token。
3. 逐项检查验收条件和严重错误，再填写维度分数；当前任务不会自动调用模型或自动裁判。
4. 失败时可记录重试；重试保留旧证据，不增加独立重复次数。
5. 查看覆盖率、质量分、首次通过率与合格成本，最后导出汇总 Excel、明细 Excel、Markdown 或 JSON。

普通对话的整体 1–5 分不同于任务内的多维加权分。从普通对话创建的任务副本当前标为演示来源；正式测评需新增独立记录。评分公式、默认权重及缺失值处理见 [测评任务与报告](docs/测评任务与报告.md)。

## 7. 保存与下次继续

浏览器保存本地副本，并尝试同步至后端 SQLite；当前没有完整同步冲突处理和失败重试保障。本地保存成功不保证远端备份成功，重要结果应及时导出。

生成媒体落盘和数据库快照需要可写存储。更换浏览器来源、端口或清理站点数据可能影响本地记录，媒体引用也可能受服务地址和文件清理影响。

C2C 入口目前提供 Single / T2T / C2C 三种方式的流程演示，尚未接入真实缓存推理后台，不用于实际性能比较。

功能全览见 [README](README.md)，启动及常见问题见 [安装与运行](docs/安装与运行.md)。
