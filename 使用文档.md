# 对照 DUIZHAO · 使用文档

> 产品定位：面向 AI 产品经理的「AI 方案对比台」。把同一道业务问题交给多个云端模型分别回答，用于模型选型、提示词调优与需求验收。
>
> 本使用文档对应**当前已实现版本**（含真实模型调用、图片/视频生成、媒体落盘、线上 TOS 持久化）。

---

## 一、快速开始

安装与前后端启动见 [README](README.md#安装方法)。本地前端默认 `http://127.0.0.1:8768/arena.html`，后端默认 `http://127.0.0.1:8000`。

首次使用先配置模型的服务地址、模型 ID 和 API Key，再发起对比。请检查卡片的数据来源标记，演示结果不能作为实测结论。

---

## 二、使用步骤

### 第 1 步：配置模型

1. 点击左侧导航「模型配置」。
2. 点「添加模型」→ 在「预设模型」下拉选择或搜索（已内置蚂蚁 MaaS、DeepSeek、智谱、月之暗面、豆包、腾讯混元、百度文心、讯飞星火、千问AI平台等 100+ 模型）。
3. 预设会自动带出模型 ID、厂商、服务地址和能力；可继承已配置的服务商密钥，也可为独立连接单独填写 API Key；保存后请验证每个连接。
4. 保存后勾选要参与对比的模型（普通文本对比 2～9 个，图片/视频生成至少 1 个）。


### 第 2 步：发起对比

1. 在输入框输入一道业务问题。
2. 通过「内容类型」切换：**文本 / 图片生成 / 图片理解 / 代码 / 视频**。
3. 选择参与对比的模型（或直接使用已选模型）。
4. 点右下角发送按钮。



### 第 3 步：查看结果与评价

- 各模型**独立流式输出**，一个模型慢或失败不影响其它卡片。
- 图片/视频结果直接展示，点击可看大图/播放；生成完成后卡片会显示真实耗时（满 60 秒自动转为「X 分钟」）。
- 每张卡片可点「更好 / 较差」进行单项评价，并填写评分与备注。


### 第 4 步：连接测试与导出

- 「模型配置 → 连接测试」可对单个模型发起一次最小真实调用，验证地址/密钥/模型 ID 是否正确（图片/视频模型请在对应模式发送一次真实生成来验证）。
- 「导出报告」支持 Markdown / CSV / XLSX，导出列包含问题、方案、模型、提示词、回答、耗时、Token、费用、媒体引用与评价。


---

## 三、内容类型说明

| 类型 | 用途 | 参数 |
|---|---|---|
| 文本 | 同一问题比较多个模型的文字回答 | 2～9 个模型 |
| 图片生成 | 描述画面比较生成图片；可上传参考图做图生图（i2i） | 分辨率 1024×1024 / 1080P / 2K / 4K |
| 图片理解 | 上传图片，比较各模型的理解与描述 | 需先上传一张图片 |
| 代码 | 描述需求，比较代码实现 | — |
| 视频 | 描述镜头生成视频；可上传首帧做图生视频（i2v） | 分辨率 720P / 1080P + 时长 |


---

## 四、其它功能

- **提示词库**：保存常用系统提示词与案例问题，一键带入新对比。
- **测评任务**：按固定模型/用例/权重创建人工测评任务，录入结果、评价与证据，导出 Markdown / 汇总 Excel / 明细 Excel / JSON；任务本身不自动调用模型。
- **C2C 协作**：单模型、文字协作、缓存协作三种方式的交互演示（真实缓存通信需 GPU 后台，当前为演示）。
- **明暗主题**：右上角切换，自动记忆。
- **数据可靠性**：支持媒体落盘与可选 SQLite 快照持久化；需要实际配置存储挂载，且当前备份失败不会自动告警，重要结果应另外导出保存。

---

费用按内置人民币价格表估算，未计价或缺少 Token 时不能用显示金额替代账单。评分规则见 [测评任务与报告](docs/测评任务与报告.md)。

## 五、参考文档

- 当前开发说明：[开发说明](docs/开发说明.md)
