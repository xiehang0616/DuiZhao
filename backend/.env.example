# 各模型 API Key（留空则后端以 mock 流式返回示例；也可在网页「连接设置」里输入保存）
KEY_ANT=
KEY_DEEPSEEK=
KEY_ZHIPU=
KEY_MOONSHOT=
KEY_DOUBAO=

# 后端端口（默认 8000）
PORT=8000

# 数据库路径（本地默认 backend/data/duizhao.db；线上 veFaaS 设为 /tmp，例如下面这行）
# DATABASE_URL=sqlite:////tmp/data/app.db
